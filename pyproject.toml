[project]
name = "financial-simulation"
version = "0.1.0"
description = "A new pywire application"
requires-python = ">=3.12"
dependencies = [
    "pywire>=0.1.9",
    "stripe>=7.0.0",
    "sqlalchemy>=2.0.0",
    "yfinance>=0.2.0",
    "pandas>=2.0.0",
    "fredapi>=0.5.0",
]
